import { Button } from "@mui/material";

const Index = () => {
  return (
    <div>
      <Button variant="contained">New Post</Button>
      Posts index
    </div>
  );
};

Index.auth = true;

export default Index;
